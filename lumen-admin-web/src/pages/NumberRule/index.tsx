/**
 * NumberRule page — placeholder.
 * Full number-rule (业务编号生成) implementation lands in Task 8.3.
 */
import { Card, Typography } from 'antd';

export default function NumberRule() {
  return (
    <Card>
      <Typography.Title level={4}>编号规则</Typography.Title>
      <Typography.Paragraph type="secondary">
        NumberRule page — full impl in Task 8.3.
      </Typography.Paragraph>
    </Card>
  );
}
