/**
 * Lumen Admin — App shell.
 *
 * NOTE: This is a minimal placeholder for Task 8.1 (project initialization).
 *       The full router wiring, BasicLayout, Login page, and 7 backend pages
 *       will be added in Task 8.2 (request / access / layout / login) and
 *       Task 8.3 (7 backend pages).
 */
function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Lumen Admin Skeleton</h1>
      <p>Bootstrap complete — Vite + React + TS + Antd Pro ready.</p>
      <p>
        Next: Task 8.2 (request / access / BasicLayout / Login) and Task 8.3
        (7 backend pages).
      </p>
    </div>
  );
}

export default App;